# Storage

This directory represents the root of an orphan alphabook branch, not a source
checkout. Code lives on separate branches. Task records are under `tasks`;
decisions explain why the project made its current choices.
