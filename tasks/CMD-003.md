---
kind: task
id: CMD-003
title: Build the local project planning web application
status: done
depends_on:
  - CMD-002
paths:
  - apps/server.ts
  - apps/web
  - tests/server.test.ts
  - tests/browser.spec.ts
artifacts:
  - artifacts/CMD-003-validation.md
---

Register projects and browse tasks, their dependency graph, decisions, documents,
artifacts, worktrees and task-linked commits. Refresh changes made with ordinary
file tools. Inherit the existing Command Center's visual styling only, not its
layout, architecture or agent execution features.

Acceptance: isolated API and browser tests; usable locally; old app unaffected.

Delivered a loopback-only web app with project registration, accepted/live
snapshots, task filters and graph, decisions, docs, artifacts, commit trailers and
worktree comparisons. Shared visual palette, controls and OFL-licensed DINish
font; new layout and application code. Registered Command as its own project.
