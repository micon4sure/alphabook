---
kind: task
id: CMD-002
title: Read registered projects and worktree snapshots from files and Git
status: done
depends_on:
  - CMD-001
decisions:
  - D-001
  - D-002
  - D-003
paths:
  - packages/core
  - tests/core.test.ts
artifacts:
  - artifacts/CMD-002-validation.md
---

Build the shared reader and machine-local project registry. Keep checkout files
separate from the accepted integration-branch Git tree. Validate the draft format,
resolve dependencies, discover worktrees, and find task-linked commits.

Acceptance: real Git/worktree tests, strict metadata validation, path containment,
clone-safe registration, external file changes visible without a database.

Delivered a shared TypeScript reader and local registry; 17 passing tests plus
the 14 independent Python format tests. Accepted snapshots read immutable Git
blobs, even when the selected worktree manifest has uncommitted invalid edits.
