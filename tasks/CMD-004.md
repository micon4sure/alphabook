---
kind: task
id: CMD-004
title: Expose project planning queries through an optional MCP adapter
status: done
depends_on:
  - CMD-002
  - CMD-003
paths:
  - apps/mcp.ts
  - tests/mcp.test.ts
  - README.md
  - spec/mcp.md
artifacts:
  - artifacts/CMD-004-validation.md
---

Provide a standalone stdio MCP service using the shared file/Git reader. Query
registered projects, worktrees, open and ready tasks, decisions, docs, validation
and task-linked commits without needing the web server. Document installation
and the file-first workflow. Planning writes will be a separate verified task;
this first service must clearly advertise itself as read-only.

Acceptance: real SDK client/stdio integration tests, documented setup and limits.

Delivered 11 explicitly read-only tools and a project-list resource. Real SDK
client tests verify negotiation, queries, context isolation, file-change refresh,
validation diagnostics and access scoping without running a web server.
